# Orchestration (Airflow is typically installed via Docker image, but adding for reference if running locally without docker)


# Extraction
fastf1==3.3.5

# Transformation
pandas==2.2.2
numpy==1.26.4

# Storage / Database Interaction
SQLAlchemy>=2.0.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1  # For managing Supabase/Neon credentials

# Presentation
streamlit==1.35.0
plotly==5.22.0
requests==2.31.0